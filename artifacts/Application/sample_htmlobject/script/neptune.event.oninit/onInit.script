oHTMLObject.setContent("<div> This is the simple use case for the HTML control: Some - usually decorative - HTML fragment is specified as a string and set to the content property. Without further configuration, the HTML control will convert this string to DOM nodes whenever it has to render.</div>"
+ "<div>This example contains an HTML control. The content of the HTML control is defined by a longer string literal. Whenever the control is rendered, the string content is rendered as inner HTML and thereby converted to DOM nodes.</div>");


oHTMLObject1.setContent("<div> This is the simple use case for the HTML control with Sanitize Content = true</div>"
+ "<div>This example contains an HTML control. The content of the HTML control is defined by a longer string literal. Whenever the control is rendered, the string content is rendered as inner HTML and thereby converted to DOM nodes.</div>");